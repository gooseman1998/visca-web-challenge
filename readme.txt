To compile scss, follow these steps:

1 make sure you have sass installed, if not: run 'npm install -g sass' in your terminal
2 go to directory assets/styles
3 open terminal
4 run command 'sass scss/style.scss css/style.min.css --watch'
